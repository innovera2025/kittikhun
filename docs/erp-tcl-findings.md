# ผลสำรวจฐานข้อมูล ERP จริง (db_TCL) + การตัดสินใจ

> **ทดสอบเมื่อ:** 17 ส.ค. 2569 · **วิธี:** อ่านอย่างเดียว (SELECT/INFORMATION_SCHEMA) ไม่มีการเขียนใด ๆ ลง ERP
> เอกสารนี้เป็นข้อเท็จจริงจาก DB จริง ใช้แทนสมมติฐานทั้งหมดใน [`erp-integration.md`](erp-integration.md)

---

## 1. สภาพแวดล้อม

| | |
|---|---|
| DBMS | Microsoft SQL Server 2019 (RTM-GDR, 15.0.2155.2, x64) |
| Database | `db_TCL` |
| Collation | `Thai_CI_AS` (ทั้ง server และ database) |
| ชนิดคอลัมน์ข้อความ | **NVARCHAR 2,881 · NCHAR 75** vs VARCHAR 138 · CHAR 9 · TEXT 80 |
| ผลต่อคอนฟิก | ✅ **`ERP_SQL_CHARSET=utf8`** — ไม่ต้องแปลง TIS-620/Windows-874 |
| ERP | ระบบบัญชี/ERP ไทย มีโมดูลสต็อกและ**โมดูลนับสต็อกใช้งานจริงอยู่แล้ว** |

---

## 2. ข้อกำหนดด้านความปลอดภัยก่อนขึ้นจริง

> ℹ️ สภาพการตั้งค่าปัจจุบันของ ERP ถูกตัดออกจากเอกสารฉบับนี้โดยตั้งใจ (repo เป็น public)
> รายการตรวจฉบับเต็มพร้อมสถานะจริงอยู่กับผู้ดูแลระบบภายใน — ที่นี่เก็บเฉพาะ
> **ข้อกำหนดที่ระบบนี้บังคับ** ซึ่งจำเป็นต่อการอ่านโค้ดให้เข้าใจ

| # | ข้อกำหนด | บังคับด้วยอะไร |
|---|---|---|
| 1 | login ที่ต่อ ERP ต้องมีสิทธิ์ **`db_datareader` เท่านั้น** (หรือ GRANT SELECT เฉพาะ object ที่ใช้) — บัญชีระดับ `sysadmin`/`db_owner` ใช้ไม่ได้ | boot probe ใน `MssqlDriver.verifyReadOnly()` ถามสิทธิ์จาก metadata ล้วน (ไม่เขียนอะไรลง ERP) · พบสิทธิ์เขียน = **ปฏิเสธการ start** |
| 2 | พอร์ตของ ERP ต้องเข้าถึงได้เฉพาะจากเซิร์ฟเวอร์แอป — จำกัดด้วย firewall หรือเชื่อมผ่าน VPN/LAN | นอกขอบเขตโค้ด — งานของผู้ดูแลระบบ |
| 3 | ตั้ง `ERP_SQL_ENCRYPT=true` (+ `ERP_SQL_TRUST_SERVER_CERT=true` ถ้าเป็น self-signed) เพื่อไม่ให้ credential/ข้อมูลวิ่งแบบไม่เข้ารหัส | คอนฟิกที่ระดับ environment — ค่า default คือ `false` จึงต้องตั้งเอง |
| 4 | ค่าเชื่อมต่อจริงต้องไม่หลุดเข้า repo | ✅ `.gitignore` กันไฟล์ environment ทุกแบบ + `config/` — ตรวจแล้วว่าไม่มีไฟล์ความลับถูก track |

---

## 3. ตารางสำคัญที่พบ

| ตาราง | แถว | บทบาท |
|---|---|---|
| **`InventoryItem`** | 533 | **item master** — แหล่งข้อมูลสินค้าหลัก |
| `InventoryItem_11082026`, `_17072026`, `_Old` | 533 | สำเนา backup — **ห้ามใช้** |
| **`InventoryFlowDtl`** / `InventoryFlowHdr` | 370 | **movement transaction** — แหล่งคำนวณยอดคงเหลือจริง (มีคอลัมน์ `Location` ด้วย) |
| **`tbl_CountHdr`** / **`tbl_CountDtl`** | 189 | **โมดูลนับสต็อกของ ERP — ใช้งานจริง** |
| `tbl_STOCKSTD` | 231 | ledger เก่า — รหัสสินค้าคนละระบบ **ใช้ไม่ได้** (ดู §5) |
| `tbl_STOCKAVG` | 0 | ว่าง |
| `tbl_StockFiFo` | 76 | 32 รายการ รหัสคนละระบบเช่นกัน |
| `InventoryLedgers` | 0 | ว่าง |
| `z_ItemCodeNew` | 214 | ตาราง Min/Max ต่อ SKU (**ไม่ใช่**ตารางแปลงรหัส) — อาจใช้เสริม ROP ได้ |
| `cc` | 97,486 | ตารางใหญ่สุด แต่เป็นข้อมูลราคา/ต้นทุน ไม่เกี่ยวกับการนับ |

**Stored procedures ที่ ERP ใช้คำนวณสต็อก:** `sp_CountStk`, `sp_BalanceSHELF`, `sp_COUNTSHELF`, `sp_StockCardSHELF`, `sp_StockInOut`, `sp_StockMOVFIFO`, `spSTOCK_AVG_COST*`

---

## 4. ความครบของข้อมูลเทียบกับฟิลด์ที่ design ต้องใช้

จาก `InventoryItem` ทั้ง 533 แถว (ทุกแถว `IsActive=1` และ `IsStock=1`):

| ฟิลด์ที่หน้าจอใช้ | คอลัมน์ ERP | ครบ | สรุป |
|---|---|---|---|
| SKU | `ItemCode` | **100%** (533) | ✅ ใช้ได้ |
| ชื่อสินค้า (ไทย) | `ItemName` nvarchar(200) | **100%** | ✅ ใช้ได้ |
| หน่วย | `MainUnits` (กส., ถัง, กล่อง, มัด) | **100%** | ✅ ใช้ได้ |
| ประเภท | `ItemType` / `ItemTypename` (วัตถุดิบ ฯลฯ) | 100% | ✅ ใช้จัดกลุ่มได้ |
| บาร์โค้ด | `BarCodeUnits` / `BarCodePack` | **1.9%** (10 / 9) | 🔴 → ตัดสินใจแล้ว §6.1 |
| ตำแหน่งเก็บ (loc) | `Shelf` | **0%** | 🔴 → รอสรุป §6.4 |
| ชื่ออังกฤษ | `ItemNameEng` | **0%** | ตัดออกจาก UI |
| จุดสั่งซื้อ (ROP) | `MinStock` | **26.8%** (143) | ⚠️ ต้องมีกติกา fallback |
| จอง (reserved) | `PendingQTY` | **0%** | ตัด tile "พร้อมขาย"/"จอง" |
| ล็อต | `LotNumber` | **0%** | ตัดจาก spec rows |

**บาร์โค้ด 10 รายการที่มีอยู่เป็น EAN-13 ถูกต้อง** — prefix `885…` (ประเทศไทย) เช่น `8859689000250`; `BarCodePack` เป็นรุ่น 14 หลัก (ITF-14 pack) เช่น `18859689000257` → ทั้งสองแบบใช้ร่วมกับ Code128 ของ ItemCode ได้

---

## 5. ⚠️ ยอดคงเหลือ: ไม่มีตารางที่เก็บยอดสำเร็จรูปไว้

หลักฐานที่ตรวจได้:

| แหล่งที่ลอง | ผล |
|---|---|
| `InventoryItem.BalStock` | มีค่าไม่เป็นศูนย์แค่ **8 / 533** และเทียบกับ ledger **ไม่ตรงเลย (0 จาก 56)** → ใช้ไม่ได้ |
| `tbl_STOCKSTD` | มีแค่ 56 รายการ · **รหัสสินค้าเป็นระบบอื่นคนละชุด: match `ItemCode` = 0, `ForItemCode` = 0, `PartCode_OLD` = 0, `CustRefCode` = 0** (เช่น `4R02001-1`, `340206-AL0205`, `R04-0002` เทียบกับ `1010001`, `2010201`) · ข้อมูลค้างปี 2024–2025 → ใช้ไม่ได้ |
| `tbl_STOCKAVG`, `InventoryLedgers` | ว่างเปล่า |
| **`InventoryFlowDtl`** | ✅ สินค้าที่ ERP เพิ่งนับ (`2010404` สารส้ม, ระบบบอก 262) **มี 9 movement ในตารางนี้** — และมีคอลัมน์ `ItemCode, Warehouse, Location, MainQuantity, MainUnits, InOut, ReasonIndex` ครบสำหรับคำนวณ |

**สรุป:** ยอดคงเหลือคำนวณจาก movement (`InventoryFlowDtl`) ตามเครื่องหมาย `InOut` — หรือผ่าน stored procedure ของ ERP เอง
👉 **การตัดสินใจ: ใช้ script ดึง Inventory ที่เจ้าของโปรเจคจะส่งมอบ เป็นสูตร/สัญญาที่ถูกต้อง** (ไม่เดาตรรกะ `InOut`/`ReasonIndex` เอง)

---

## 6. การตัดสินใจของเจ้าของโปรเจค (17 ส.ค. 2569)

### 6.1 บาร์โค้ด → **สแกน `ItemCode` เป็น Code128** ✅

- พิมพ์ฉลาก **Code128 จาก `ItemCode`** (มีครบ 100%) ติดชั้น/กล่อง → ใช้งานได้ทั้ง 533 รายการทันที ไม่ต้องแก้ข้อมูลใน ERP
- บาร์โค้ด EAN-13 เดิม 10 รายการ **ยังอ่านได้ด้วย** (`mobile_scanner` เปิดทั้ง `code128 + ean13 + ean8 + code39 + qrCode` ตาม design อยู่แล้ว)
- ตาราง `item_barcodes` ในระบบเรารองรับ 1 SKU : N barcode → ใส่ทั้ง ItemCode-Code128 และ EAN-13 จริงได้พร้อมกัน
- **งานที่เพิ่ม:** ฟีเจอร์พิมพ์ฉลาก Code128 (จะแยกเป็นงานย่อยในแผน implement)

### 6.2 ยอดคงเหลือ → **รอ script ของเจ้าของโปรเจคเป็นตัวกำหนด** ✅

- Driver `sql` จะรัน `.sql` จาก script นั้นตามรอบ (`ERP_SQL_ITEMS_SQL_FILE` — ยอดคงเหลือมาพร้อม item master ใน query เดียว ไม่มีไฟล์/รอบแยกสำหรับยอด)
- สิ่งที่ต้องดูจาก script เมื่อได้รับ: สูตรยอดคงเหลือ, การกรองคลัง, คอลัมน์ที่ให้มาครบไหม, เวลาที่ใช้รัน

### 6.3 รอบนับ → **ดึงรอบนับ/รายการจาก ERP มาให้พนักงานนับในมือถือ** ✅

- แหล่งรอบนับ = `tbl_CountHdr` (VoucherNo, CountDate, Emp_ID, CountNo, CountYear) + `tbl_CountDtl` (ItemCode, Warehouse, MainQty = ยอดระบบ, MainUnits)
- **รหัสสินค้าใน `tbl_CountDtl` match `InventoryItem.ItemCode` 96/96 = 100%** ✅ เชื่อมได้สะอาด
- แอปเราแทนที่ขั้นตอน "กรอกจำนวนที่นับได้" ให้ทำบนมือถือ แล้ว**เก็บผลและส่วนต่างในระบบเรา** (ไม่เขียนกลับ ERP ตามที่ตัดสินใจไว้)
- ผลจาก `tbl_CountDtl` เดิม (`CountQty`, `DifQty`) ใช้เป็นข้อมูลอ้างอิง/ประวัติได้
- รอบนับที่มีอยู่จริงในระบบ:

| VoucherNo | วันที่ | รายการ | มีส่วนต่าง |
|---|---|---|---|
| `CNT-2608-0003` | 17/08/2026 | 96 | 27 |
| `CNT-2608-0001` | 15/08/2026 | 186 | 108 |
| `CNT-2608-0002` | 15/08/2026 | 0 | 0 |

- **ผลต่อสถาปัตยกรรม:** `count_sessions` / `count_snapshot` ในระบบเราเปลี่ยนจาก "admin สร้างเอง" เป็น **mirror ของรอบนับ ERP** (frozen qty = `tbl_CountDtl.MainQty` ณ เวลาที่ดึง) — ยังคงกลไก outbox / idempotency / ส่วนต่างคำนวณที่ server ไว้ทั้งหมด

### 6.4 ตำแหน่งเก็บ / คลัง → ⏳ **รอความชัดเจน**

ข้อเท็จจริงที่ทราบ:
- `Shelf` ว่าง 100% แต่ **`InventoryFlowDtl` มีคอลัมน์ `Location`** (ยังไม่ได้ตรวจความครบ)
- คลังที่พบใน movement: **`WHRM`** (วัตถุดิบ) · **`WHFG`** (สินค้าสำเร็จรูป) · **`WHWIP`** (ระหว่างผลิต) · **`WHNG`** — **ไม่ใช่ `WH-BKK-02`** ที่ design สมมติไว้ → ต้องแก้ค่า `WAREHOUSE_CODE` และรองรับหลายคลัง
- `tbl_CountDtl` มีคอลัมน์ `Warehouse` ต่อรายการ (ตัวอย่างรอบล่าสุดเป็น `WHFG`)

---

## 6.5 ⭐ ผลทดสอบ "ItemCode + Qty มีพร้อมใช้ไหม" (17 ส.ค. 2569 บ่าย)

### สรุป: **ItemCode ครบ · Qty มี 96 จาก 620 รายการ (15%)**

| แหล่ง qty | ครอบคลุม | ใช้ได้ไหม |
|---|---|---|
| **`tbl_CountDtl.MainQty`** | **96 รายการ** (รอบนับ CNT-2608-0003) | ✅ **ใช้ได้ทันที** — ไม่มี NULL, มีหน่วย+คลังมาด้วย, รหัส match item master 100% |
| คำนวณจาก `InventoryFlowDtl` | 101 รายการมี movement | ❌ **ยังไม่มีสูตรที่ถูก** — ทดสอบ 7 สูตร ดีที่สุดตรงแค่ 52.1% |
| `InventoryItem.BalStock` | 8 รายการ | ❌ |
| `tbl_STOCKSTD.QTY_BALANCE` | 56 รายการ (รหัสคนละระบบ) | ❌ |

**ผลทดสอบสูตรคำนวณจาก `InventoryFlowDtl` เทียบกับ `MainQty` 96 รายการ (ค่าอ้างอิงที่ ERP ถือว่าถูก):**

| สูตร | ตรงกัน |
|---|---|
| `SUM(MainQuantity × InOut)` (ทุกแถว / Approved=1 / IsStock=1 / คลัง WHFG) | ⚠️ 50/96 = **52.1%** |
| `SUM(MainQuantity)` ไม่คูณเครื่องหมาย | 23/96 = 24.0% |
| `SUM(RecQTY) − SUM(IssueQTY)` | 10/96 = 10.4% |
| `SUM(BalQTY)` | 10/96 = 10.4% |

→ `InventoryFlowDtl` มีแค่ 406 แถว / 101 สินค้า **ไม่ครอบคลุมยอดยกมา (opening balance)** จึงคำนวณยอดจริงไม่ได้
→ **ยังต้องได้ query/สูตรจากฝั่ง ERP** ถ้าต้องการ qty ครบทุกรายการ

---

## 6.6 ✅ ได้สูตรยอดคงเหลือจากฝ่าย ERP แล้ว (22 ส.ค. 2569)

ฝ่าย ERP ส่ง query ที่ **ตัว ERP ใช้เอง** มาให้ — เป็นสูตรที่ 8 ต่อจาก 7 สูตรที่ลองเองใน §6.5

```
SUM(InventoryFlowDtl.InOut × InventoryFlowDtl.MainQuantity)
FROM InventoryFlowHdr LEFT OUTER JOIN InventoryFlowDtl
  ON Hdr.TranSactionno = Dtl.Transactionno AND Hdr.VoucherNo = Dtl.VoucherNo
WHERE Dtl.ItemCode = ? AND Hdr.InOutDate <= ? AND Hdr.Approved = 1
  AND Hdr.IsClosed <> 1 AND Dtl.Warehouse = ?
```

**ต่างจากสูตรที่ลองเองอย่างไร** — สูตรเดิมที่ได้ 52.1% ขาด 3 อย่างนี้:
`IsClosed <> 1` · join ด้วย **ทั้ง** `TranSactionno` และ `VoucherNo` (เดิมใช้คีย์เดียว) · กรองคลังที่ระดับ `Dtl`

**นำเข้าระบบแล้ว:** แปลงเป็น T-SQL แบบ set-based (ทุกรหัสในครั้งเดียว) รวมกับ query item master
ที่ [`server/sql/erp/inventory-items-with-balance.sql`](../server/sql/erp/inventory-items-with-balance.sql)
→ ไหลเข้า `CanonicalItem.onHand` ผ่าน `fetchItems()` · ล็อกเงื่อนไขไว้ด้วยเทสต์ 25 เคส
(`server/test/erp-items-script.spec.ts`) — แก้เงื่อนไขโดยไม่คุยกับฝ่าย ERP = เทสต์แดงทันที

### ✅ ตรวจกับ `db_TCL` จริงแล้ว — สูตรถูกต้อง **100%** (22 ส.ค. 2569)

ต่อ `db_TCL` จริง (SQL Server 2019 Enterprise · `Thai_CI_AS`) แบบ **อ่านอย่างเดียว**
แล้วเทียบยอดที่สูตรคำนวณได้ กับ `tbl_CountDtl.MainQty` ซึ่ง §6.5 ถือเป็นค่าที่ ERP ยอมรับว่าถูก

| รอบนับ | คลัง | วันที่นับ | ตรงกัน |
|---|---|---|---|
| CNT-2608-0001 | WHFG | 15 ส.ค. | **93/93 = 100%** |
| CNT-2608-0003 | WHFG | 17 ส.ค. | **96/96 = 100%** |
| CNT-2608-0004 | WHFG | 17 ส.ค. | 80/100 = 80% (ดูหมายเหตุ) |

**⭐ สาเหตุที่สูตรเดิมเคยได้แค่ 52.1% ไม่ใช่เพราะสูตรผิด — แต่เพราะ "จุดตัดเวลา" ผิด**

`tbl_CountHdr.CountDate` ไม่มีเวลาติดมา (เป็น `00:00:00` เสมอ) → เงื่อนไข `InOutDate <= CountDate`
ของฝ่าย ERP หมายถึง **"ยอดถึงสิ้นวันก่อนหน้าวันนับ"** ไม่ใช่ "รวมความเคลื่อนไหวของวันที่นับด้วย"

ทดสอบไล่ `asOf` ทีละวันยืนยันชัด (CNT-2608-0003, 96 รหัส):

| asOf | ตรงกัน |
|---|---|
| สิ้นวัน 14 ส.ค. | 33.3% |
| **สิ้นวัน 15 ส.ค.** | **100%** |
| **สิ้นวัน 16 ส.ค.** | **100%** (ไม่มี movement วันที่ 16 จึงเท่ากัน) |
| สิ้นวัน 17 ส.ค. (วันนับ) | 46.9% |
| สิ้นวัน 18 ส.ค. | 39.6% |

→ **สูตรของฝ่าย ERP ใช้ได้เลย ไม่ต้องแก้** · สำหรับ sync ยอดปัจจุบันเข้า `items_cache`
ให้ส่ง `@asOf = เวลาปัจจุบัน` ตามที่ driver ทำอยู่แล้ว

**หมายเหตุ CNT-2608-0004 (80%):** รอบนี้ `CountDate` = 17 ส.ค. แต่ `EntryDate`/`VoucherDate` = 19 ส.ค.
คือถูก**คีย์เข้าระบบทีหลัง 2 วัน** ยอดที่ freeze ไว้จึงเป็นยอด ณ ตอนคีย์ ไม่ใช่ตอนนับ
(ตรงมากที่สุดที่ `asOf` = สิ้นวัน 17 ส.ค. = 80%) — เป็นลักษณะการกรอกข้อมูล ไม่ใช่ข้อบกพร่องของสูตร

### ประเด็นที่ปิดแล้วจากการตรวจจริง

| ประเด็นที่เคยกังวล | ผลตรวจจริง |
|---|---|
| `IsClosed <> 1` ตัดแถว NULL ทิ้ง | ✅ ไม่เป็นปัญหา — `db_TCL` มีแถว `IsClosed` เป็น NULL **0 แถว** (และ `Approved` NULL 0 แถว) |
| ยอดยกมาหายไป | ✅ ไม่เป็นปัญหา — ตรง 100% แสดงว่ายอดยกมาอยู่ใน ledger ครบแล้ว · ยอดต่ำสุดที่คำนวณได้ = 0 ไม่มีติดลบ |

### 🔴 ประเด็นใหม่ที่พบ — ต้องตัดสินก่อน go-live

**1. คลัง `WHRM` คำนวณยอดจาก ledger ไม่ได้**

`InventoryFlowDtl` มีแถวของ WHRM แค่ **4 แถว** (เทียบกับ WHFG 802 แถว) แต่ WHRM มีรอบนับจริง
2 รอบ (CNT-2608-0005 106 บรรทัด · CNT-2608-0006 114 บรรทัด) ที่มี `MainQty` ครบ
→ ERP ได้ยอด WHRM มาจากที่อื่นที่ไม่ใช่ ledger นี้ **ต้องถามฝ่าย ERP ว่ามาจากไหน**
ระหว่างนี้ระบบใช้ได้เฉพาะคลัง **WHFG**

**2. สินค้าที่ไม่มี movement ได้ยอด NULL**

WHFG: 552 รายการ active · มียอด 116 · **NULL 436 รายการ** — เราไม่แปลงเป็น 0 โดยตั้งใจ
ต้องยืนยันกับฝ่าย ERP ว่า "ไม่มี movement" = "ยอด 0 จริง" หรือ "ยังไม่มีข้อมูล"

**วิธีตรวจซ้ำ:** [`server/sql/erp/verify-balance.sql`](../server/sql/erp/verify-balance.sql) (อ่านอย่างเดียว)

### 🔴 ปัญหาใหม่ที่ต้องตัดสิน: `ItemCode` ซ้ำ 85 รหัส (172 แถว)

`InventoryItem` PK = `(Roworder, ItemCode)` → **ItemCode ไม่ unique** · 620 แถว แต่ ItemCode ไม่ซ้ำแค่ **533**

ตัวอย่าง — ทุกคู่เป็น "รายการเดิม (25/07)" + "รายการใหม่ชื่อโรงงาน (17/08 = วันนี้)":

| ItemCode | Roworder | ชื่อ | EntryDate |
|---|---|---|---|
| `2010404` | 193 | สารส้ม (คัดพิเศษ) บรรจุ 1 กก. | 25/07/2026 |
| `2010404` | **617** | **สารส้ม** | **17/08/2026** |
| `2021001` | 207 | น้ำตาลมะพร้าว สีลาน 1 กก. | 25/07/2026 |
| `2021001` | **536** | **ลานแข็งมัดยาง 1กก.** | **17/08/2026** |

**ผลกระทบ:** การตัดสินใจ §6.1 (สแกน ItemCode เป็น Code128) จะทำให้**สแกน 1 บาร์โค้ดแล้วเจอ 2 รายการชื่อต่างกัน** → ต้องเลือกทางใดทางหนึ่ง:
- (ก) แก้ข้อมูลใน ERP ให้ ItemCode ไม่ซ้ำ (ทางที่ถูกต้องที่สุด)
- (ข) กติกาในระบบเรา: `Roworder` สูงสุดชนะ (รายการใหม่สุด) — ต้องยอมรับความเสี่ยงว่าเลือกผิด
- (ค) แสดงทั้ง 2 รายการให้พนักงานเลือก

### 📥 ข้อมูลกำลังถูกใส่อยู่

ระหว่างการทดสอบ item master เพิ่มจาก **533 → 620 แถว** (EntryDate 17/08/2026, EntryBy `ADMIN`) · ROP เพิ่มจาก 143 → 179 ✅ · บาร์โค้ดยังคง 10 รายการ
→ ตัวเลขในเอกสารนี้เป็นภาพ ณ 17 ส.ค. 2569 อาจเปลี่ยนอีก ควรวัดซ้ำก่อน go-live

---

## 7. ผลกระทบต่อ design ที่ต้องปรับ (สรุปรวม)

| # | design เดิม | ความจริงจาก DB | การปรับ |
|---|---|---|---|
| 1 | บาร์โค้ด EAN-13 ทุกรายการ | มี 1.9% | สแกน Code128 จาก ItemCode + พิมพ์ฉลาก |
| 2 | `loc` แบบ `A-04-12` ทุกการ์ด | `Shelf` ว่าง 100% | รอสรุป §6.4 |
| 3 | บรรทัดชื่ออังกฤษใต้ชื่อไทย | ว่าง 100% | ตัดออกจาก UI |
| 4 | 3 tiles: จอง / พร้อมขาย / จุดสั่งซื้อ | `PendingQTY` ว่าง 100%, ROP 26.8% | เหลือ tile จุดสั่งซื้อ (+ซ่อนเมื่อไม่มีค่า) — ปรับ layout การ์ดขยาย |
| 5 | สถานะ พร้อมจ่าย/ใกล้หมด/หมดสต็อก จาก ROP | ROP มีแค่ 143 รายการ | ไม่มี ROP → แสดงเฉพาะ "หมดสต็อก" (ยอด ≤ 0) กับสถานะกลาง ไม่เดา "ใกล้หมด" |
| 6 | spec rows: Vendor / Lot / นับครั้งล่าสุด | Lot ว่าง 100%; vendor ต้อง join `tbl_SupItem`/`Supplier`; นับล่าสุดมีใน `tbl_CountHdr` | เหลือ vendor (ถ้า join ได้) + นับครั้งล่าสุดจากรอบนับ ERP |
| 7 | คลังเดียว `WH-BKK-02` (multi-warehouse = เฟสหลัง) | มี 4 คลังใช้งานจริง | รองรับหลายคลัง**ตั้งแต่เฟสแรก** |
| 8 | admin สร้างรอบนับเอง | ERP มีรอบนับอยู่แล้ว | mirror รอบนับจาก ERP (§6.3) |
| 9 | `ERP_SQL_CHARSET` อาจต้องเป็น win874 | NVARCHAR 95% | ใช้ `utf8` |

---

## 8. สคริปต์ทดสอบที่ใช้ (ไม่มี credential ฝังในไฟล์ — รับผ่าน env)

เก็บไว้ที่ scratchpad ของ session: `probe.js` (เชื่อมต่อ+สิทธิ์+collation) · `schema.js` (โครงสร้างตาราง) · `dataquality.js` (ความครบฟิลด์) · `onhand.js` (แหล่งยอดคงเหลือ) · `codelink.js` (การเชื่อมรหัสสินค้า)
หากต้องการเก็บถาวรในโปรเจค แนะนำวางใน `tools/db-probe/` และให้รับค่าจาก `.env` เท่านั้น
