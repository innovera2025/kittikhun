{
  "//": "tsconfig สำหรับ ESLint เท่านั้น — tsconfig.json หลัก exclude test/ ไว้ (build ไม่ต้องคอมไพล์เทสต์) แต่ lint ต้องเห็นเทสต์ด้วย",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["src/**/*", "test/**/*"],
  "exclude": ["node_modules", "dist"]
}
